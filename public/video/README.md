# Video assets

| File | Used by | Notes |
|------|---------|-------|
| `hero.mp4` | `Hero` background | Cinematic aviation loop (wing/cloud/cockpit). Muted, looping. Target ≤2–3MB, H.264. Falls back to `/images/hero.jpg` poster if absent. |

Until a real clip is added, the Hero shows the poster image + the 3D globe (which provides the motion).
